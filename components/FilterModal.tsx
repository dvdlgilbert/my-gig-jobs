import React from 'react';

const FilterModal: React.FC = () => {
    return (
        <div>
            {/* Filter modal content can be added here in the future. */}
        </div>
    );
};

export default FilterModal;
